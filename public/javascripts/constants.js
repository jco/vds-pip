/*
 * Authors: Jeff Cox, David Zhang
 * Copyright Syracuse University
 */
var Pip = Pip || {};

(function (P) {
  P.SMALL_ICON_SIZE = 15;
  P.COLLECTION_TYPES_SINGULAR = ['stage', 'factor', 'task', 'folder', 'document'];
  P.COLLECTION_TYPES = ['stages', 'factors', 'tasks', 'folders', 'documents'];
  P.HIGHLIGHT_MARGIN = 0;
  P.DEVELOPMENT = true;
})(Pip);
