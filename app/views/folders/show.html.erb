<%#
Authors: Jeff Cox, David Zhang
Copyright Syracuse University
%>

<p>Breadcrumbs: <%= breadcrumbs_for(@folder) %></p>
<p>
  <%= link_to "Create folder", new_folder_folder_path(@folder) %> &bull;
  <%= link_to "Edit folder", edit_folder_folder_path(@folder) %> &bull;
  To upload a document, double-click anywhere in the box.
</p>

<%= render :partial => "partials/pane_and_container" %>

<script type="text/javascript">
var Pip = Pip || {};
Pip.data = <%= raw(@data) %>;
Pip.data.containerId = 'container';
</script>
